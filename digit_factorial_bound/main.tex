% Label: Digit Factorial Bound

**Theorem (Digit Factorial Bound).** For every natural number $n \geq 1$, the sum of the factorials of the digits of $n$ satisfies $$\text{digitFactorialSum}(n) \leq 362880 \cdot (\lfloor \log_{10} n \rfloor + 1).$$

*Proof.* Unsolved.