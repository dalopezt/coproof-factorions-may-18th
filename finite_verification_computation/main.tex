```latex
\section*{Computation Evidence}

\textbf{Statement.} For every natural number $n \leq 2{,}540{,}160$, the digit-factorial sum satisfies $\sigma_{!}(n) = n$ if and only if $n \in \{1, 2, 145, 40585\}$, where $\sigma_{!}(n)$ denotes the sum of the factorials of the decimal digits of $n$.

\textbf{Method.} The verification was carried out by exhaustive scanning of the integer range $1 \leq n \leq 2{,}540{,}160$, partitioned into $3$ chunks distributed across $3$ cluster nodes (\texttt{node1}, \texttt{node2}, \texttt{node3}) via MPI-distributed sampling. Each node independently computed $\sigma_{!}(n)$ for its assigned chunk and reported all fixed points found.

\textbf{Evidence Summary.} All three nodes collectively identified exactly $4$ factorions in $[1, 2{,}540{,}160]$, namely $n \in \{1, 2, 145, 40585\}$, with no additional fixed points reported by any node. The evidence hash \texttt{08729b6759a34a3b} certifies the integrity of the recorded results, and the verdict was deemed \emph{sufficient}.
```