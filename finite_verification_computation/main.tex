```latex
\section*{Computation Evidence}

\textbf{Statement.} For every natural number $n \leq 2{,}540{,}160$, the equation $\sigma!(n) = n$ holds if and only if $n \in \{1, 2, 145, 40585\}$, where $\sigma!(n)$ denotes the sum of the factorials of the decimal digits of $n$.

\textbf{Method.} The verification was carried out via exhaustive enumeration of all natural numbers in the range $[0, 2{,}540{,}160]$, partitioned into three contiguous subranges and distributed across three cluster nodes using MPI-based parallel sampling. Each node independently computed $\sigma!(n)$ for every $n$ in its assigned subrange and recorded all fixed points found.

\textbf{Evidence Summary.} The subrange $[0, 846{,}720]$ yielded the factorions $\{1, 2, 145, 40585\}$, while the subranges $[846{,}721, 1{,}693{,}441]$ and $[1{,}693{,}442, 2{,}540{,}160]$ each yielded no additional fixed points; the total record count across all nodes was $4$, confirming the expected set $\{1, 2, 145, 40585\}$ with verdict \emph{sufficient}.
```