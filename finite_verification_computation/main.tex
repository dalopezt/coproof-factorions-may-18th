```latex
\section*{Computation Evidence}

\textbf{Statement.} For every natural number $n \leq 2{,}540{,}160$, the digit-factorial sum satisfies $\sigma(n) = n$ if and only if $n \in \{1, 2, 145, 40585\}$, where $\sigma(n)$ denotes the sum of the factorials of the decimal digits of $n$.

\textbf{Method.} The verification was carried out via MPI-distributed sampling across three cluster nodes (\texttt{node1}, \texttt{node2}, \texttt{node3}), partitioning the range $[0, 2{,}540{,}160]$ into chunks of $1{,}270{,}080$, $635{,}040$, and $635{,}040$ integers respectively. Each chunk was searched exhaustively for natural numbers satisfying $\sigma(n) = n$, and all results were aggregated.

\textbf{Evidence Summary.} A total of $2{,}540{,}160$ integers were checked across 3 chunk(s), yielding exactly 4 fixed points of the digit-factorial sum: $n \in \{1, 2, 145, 40585\}$; no additional solutions were found in any chunk, and the verdict was deemed \emph{sufficient}.
```