```latex
\section*{Computation Evidence}

\textbf{Statement.} The set of all natural numbers $n$ satisfying $n = \sum_{d \in \text{digits}(n)} d!$ is exactly $\{1, 2, 145, 40585\}$.

\textbf{Method.} The verification was carried out via MPI-distributed exhaustive scanning of the range $1 \leq n \leq 2{,}540{,}160$ partitioned into chunks across three cluster nodes (\texttt{node1}, \texttt{node2}, \texttt{node3}). Each chunk was independently searched for digit-factorial fixed points and results were aggregated.

\textbf{Evidence Summary.} The combined scan identified exactly four factorions: $n \in \{1, 2, 145, 40585\}$, matching the expected set in full. All $4$ records were confirmed within the expected targets, yielding a verdict of sufficient across all nodes.
```