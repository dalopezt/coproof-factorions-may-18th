```latex
\section*{Computation Evidence}

\textbf{Statement.} For every natural number $n \leq 2{,}540{,}160$, the digit-factorial sum equals $n$ (i.e.\ $n$ is a factorion) if and only if $n \in \{1, 2, 145, 40585\}$.

\textbf{Method.} The verification was carried out via exhaustive distributed sampling across three cluster nodes (\texttt{node1}, \texttt{node2}, \texttt{node3}), partitioning the range $[0, 2{,}540{,}160]$ into chunks processed independently by each rank. Each chunk was scanned to identify all $n$ for which the digit-factorial sum satisfies $\sigma_{!}(n) = n$, and the collected results were checked for containment within the expected set.

\textbf{Evidence Summary.} The scan produced a total of $4$ records, with factorions $\{1, 2, 145, 40585\}$ identified exclusively on the first rank; the remaining two ranks returned empty sets, confirming no additional factorions exist in $[0, 2{,}540{,}160]$. All discovered values were verified to lie within the expected set, yielding a verdict of \emph{sufficient}.
```