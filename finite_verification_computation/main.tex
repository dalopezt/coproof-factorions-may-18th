```latex
\section*{Computation Evidence}

\textbf{Statement.} For every natural number $n \leq 2{,}540{,}160$, the digit-factorial sum satisfies $\sigma(n) = n$ if and only if $n \in \{1, 2, 145, 40585\}$, where $\sigma(n)$ denotes the sum of the factorials of the decimal digits of $n$.

\textbf{Method.} The verification was carried out by exhaustive range-check sampling over all $n \in [0,\, 2{,}540{,}160]$, distributed across three cluster nodes (\texttt{node1}, \texttt{node2}, \texttt{node3}) via MPI-based parallelism. Each node independently computed $\sigma(n)$ and reported candidates satisfying $\sigma(n) = n$, with results aggregated and cross-checked against the expected set.

\textbf{Evidence Summary.} The scan identified exactly four factorions in $[0,\, 2{,}540{,}160]$, namely $n \in \{1, 2, 145, 40585\}$, matching the expected values precisely. The evidence record contains $4$ total qualifying entries, the verification verdict is \emph{passed}, and the evidence artifact is integrity-checked via hash \texttt{2c8cb81f5e083fde}.
```