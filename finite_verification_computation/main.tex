```latex
\section*{Computation Evidence}

\textbf{Statement.} For every natural number $n \leq 2{,}540{,}160$, the digit-factorial sum of $n$ equals $n$ if and only if $n \in \{1, 2, 145, 40585\}$.

\textbf{Method.} The verification was carried out by exhaustively scanning all integers in the range $[0,\, 2{,}540{,}160]$, computing the digit-factorial sum for each value, and checking equality; the workload was distributed across three cluster nodes (\texttt{node1}, \texttt{node2}, \texttt{node3}) via MPI-distributed sampling.

\textbf{Evidence Summary.} The scan identified exactly four factorions in $[0,\, 2{,}540{,}160]$, namely $n \in \{1, 2, 145, 40585\}$, matching the expected set in full; the empirical verdict was \emph{sufficient}, with evidence hash \texttt{49bebafa0cbed6be} and a total record count of $4$.
```