```latex
\section*{Computation Evidence}

\textbf{Statement.} For every natural number $n \leq 2{,}540{,}160$, the digit-factorial sum satisfies $\sigma(n) = n$ if and only if $n \in \{1, 2, 145, 40585\}$, where $\sigma(n)$ denotes the sum of the factorials of the decimal digits of $n$.

\textbf{Method.} The verification was carried out by an exhaustive local range check over all $n$ in $[0,\, 2{,}540{,}160]$, distributed across three cluster nodes (\texttt{node1}, \texttt{node2}, \texttt{node3}). Each node independently enumerated its assigned subrange, computing $\sigma(n)$ for every candidate and collecting all fixpoints.

\textbf{Evidence Summary.} Across all nodes a total of $4$ fixpoint records were collected, yielding the complete set $\{1, 2, 145, 40585\}$; no values outside the expected target set were encountered, and the distributed verdict was \emph{sufficient}.
```