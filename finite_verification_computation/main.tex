```latex
\section*{Computation Evidence}

\textbf{Statement.} For every natural number $n \leq 2540160$, the equation $\sigma_!(n) = n$ holds if and only if $n \in \{1, 2, 145, 40585\}$, where $\sigma_!(n)$ denotes the sum of the factorials of the decimal digits of $n$.

\textbf{Method.} The range $[0, 2540160]$ was exhaustively checked using MPI-distributed sampling across three cluster nodes (\texttt{node1}, \texttt{node2}, \texttt{node3}), with each node scanning a disjoint subrange and reporting all fixpoints of the digit-factorial sum. Results from all nodes were aggregated and cross-checked against the target set $\{1, 2, 145, 40585\}$.

\textbf{Evidence Summary.} The computation identified exactly four digit-factorial fixpoints in $[0, 2540160]$, namely $n \in \{1, 2, 145, 40585\}$, yielding a total of $4$ records. No unexpected fixpoints outside the target set were found on any node, and the cluster verdict was deemed \emph{sufficient} with evidence hash \texttt{7ab10152e0209f64}.
```