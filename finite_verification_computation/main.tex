```latex
\section*{Computation Evidence}

\textbf{Statement.} For every natural number $n \leq 2{,}540{,}160$, the digit-factorial sum satisfies $\sigma!(n) = n$ if and only if $n \in \{1, 2, 145, 40585\}$.

\textbf{Method.} The verification was carried out by an MPI-distributed exhaustive scan over all $n \in [0,\, 2{,}540{,}160]$, distributing work across three cluster nodes (\texttt{node1}, \texttt{node2}, \texttt{node3}). For each candidate, the digit-factorial sum was computed and checked for equality with $n$.

\textbf{Evidence Summary.} The scan produced exactly four fixed points of the digit-factorial sum in the checked range: $n \in \{1, 2, 145, 40585\}$, matching the expected values in full. With all $4$ records confirmed and the verdict marked sufficient, the verification passed with evidence hash \texttt{5bcdc6ca1071fbf0}.
```