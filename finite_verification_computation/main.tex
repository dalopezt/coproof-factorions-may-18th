```latex
\section*{Computation Evidence}

\textbf{Statement.} For every natural number $n \leq 2{,}540{,}160$, the digit-factorial sum of $n$ equals $n$ if and only if $n \in \{1, 2, 145, 40585\}$.

\textbf{Method.} The verification was carried out by an MPI-distributed computation across three cluster nodes, exhaustively scanning all integers $n$ in the range $[0,\, 2{,}540{,}160]$ and checking whether the digit-factorial sum satisfies $\text{digitFactorialSum}(n) = n$.

\textbf{Evidence Summary.} The scan confirmed that the complete set of factorions in $[0,\, 2{,}540{,}160]$ is exactly $\{1, 2, 145, 40585\}$, matching the expected values; the verification verdict was \emph{sufficient} with evidence hash \texttt{d1ee46a01711f8b6}.
```