```latex
\section*{Computation Evidence}

\textbf{Statement.} For every natural number $n \leq 2{,}540{,}160$, the digit-factorial sum equals $n$ if and only if $n \in \{1, 2, 145, 40585\}$.

\textbf{Method.} The range $1$ through $2{,}540{,}160$ was partitioned into $3$ chunks and scanned in parallel across three MPI-distributed cluster nodes (\texttt{node1}, \texttt{node2}, \texttt{node3}), each independently computing the digit-factorial sum for every integer in its assigned chunk. Results from all nodes were collected and merged to form the complete set of fixed points.

\textbf{Evidence Summary.} Across all $3$ chunks covering $n \in [1,\, 2{,}540{,}160]$, exactly $4$ fixed points were identified: $n \in \{1, 2, 145, 40585\}$, with no additional candidates found in the remaining chunks; the verdict was deemed \emph{sufficient}.
```