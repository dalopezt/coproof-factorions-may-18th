```latex
\section*{Computation Evidence}

\textbf{Statement.} For every natural number $n \leq 2{,}540{,}160$, the digit-factorial sum satisfies $\sigma(n) = n$ if and only if $n \in \{1, 2, 145, 40585\}$, where $\sigma(n)$ denotes the sum of the factorials of the decimal digits of $n$.

\textbf{Method.} The verification was carried out via exhaustive MPI-distributed scanning of all natural numbers in the range $[0, 2{,}540{,}160]$, partitioned into chunks distributed across three cluster nodes (\texttt{node1}, \texttt{node2}, \texttt{node3}). Each node independently computed $\sigma(n)$ for every $n$ in its assigned chunk and reported all fixed points found.

\textbf{Evidence Summary.} Across all chunks and ranks, exactly four records were returned: the factorions $1$, $2$, $145$, and $40585$, with no additional fixed points detected in the remaining chunks, yielding a verdict of \emph{sufficient} with evidence hash \texttt{c24f33f5d841ba5d}.
```