```latex
\section*{Computation Evidence}

\textbf{Statement.} The computation verifies that for every natural number $n \leq 2{,}540{,}160$, the digit factorial sum equals $n$ if and only if $n \in \{1, 2, 145, 40585\}$.

\textbf{Method.} The verification was carried out via exhaustive MPI-distributed scanning of all natural numbers in $[0, 2{,}540{,}160]$, partitioned into chunks of $846{,}720$ numbers each and distributed across three cluster nodes. Each node independently identified all $n$ in its chunk satisfying $\sigma_{!}(n) = n$ and confirmed containment within the expected set.

\textbf{Evidence Summary.} Across all three nodes, a combined total of $2{,}540{,}160$ values were scanned, yielding exactly four factorions: $\{1, 2, 145, 40585\}$. All discovered factorions were confirmed to lie within the expected set, with no unexpected values found; the verdict was deemed sufficient with $4$ total records collected.
```