```latex
\section*{Computation Evidence}

\textbf{Statement.} The set of natural numbers $n$ satisfying $\sigma!(n) = n$, where $\sigma!$ denotes the sum of the factorials of the decimal digits of $n$, is exactly $\{1, 2, 145, 40585\}$.

\textbf{Method.} The verification was carried out via MPI-distributed exhaustive sampling across three cluster nodes (\texttt{node1}, \texttt{node2}, \texttt{node3}), partitioning the search into independent ranges; each candidate $n$ in the assigned range was tested by computing $\sigma!(n)$ and checking equality with $n$.

\textbf{Evidence Summary.} The range $[0,\, 635040]$ yielded the factorions $\{1, 2, 145, 40585\}$, while the complementary range $[1270081,\, 1905120]$ yielded no additional solutions; a total of $4$ records were confirmed, and the verdict was deemed sufficient.
```