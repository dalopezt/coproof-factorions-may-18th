```latex
\section*{Computation Evidence}

\textbf{Statement.} Every natural number $n \leq 2{,}540{,}160$ satisfies the digit-factorial fixed-point property $\sigma(n) = n$ if and only if $n \in \{1, 2, 145, 40585\}$, where $\sigma(n)$ denotes the sum of the factorials of the decimal digits of $n$.

\textbf{Method.} The verification was carried out via MPI-distributed exhaustive enumeration over all natural numbers in the range $[0, 2{,}540{,}160]$, partitioned into $2$ chunks of $1{,}270{,}080$ numbers each and processed in parallel across three cluster nodes (\texttt{node1}, \texttt{node2}, \texttt{node3}). For each number in the range, $\sigma(n)$ was computed explicitly and checked for equality with $n$.

\textbf{Evidence Summary.} Across both chunks, a combined total of $2{,}540{,}160$ candidate values were checked, yielding exactly $4$ digit-factorial fixed points: $n \in \{1, 2, 145, 40585\}$. No additional fixed points were found in the second chunk, confirming that the complete solution set in $[0, 2{,}540{,}160]$ is $\{1, 2, 145, 40585\}$.
```