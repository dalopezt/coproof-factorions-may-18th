```latex
\section*{Computation Evidence}

\textbf{Statement.} For every natural number $n \leq 2{,}540{,}160$, the digit-factorial sum satisfies $\sigma(n) = n$ if and only if $n \in \{1, 2, 145, 40585\}$, where $\sigma(n)$ denotes the sum of the factorials of the decimal digits of $n$.

\textbf{Method.} The verification was carried out by exhaustive enumeration of all natural numbers in the range $[0, 2{,}540{,}160]$, distributed across three cluster nodes (\texttt{node1}, \texttt{node2}, \texttt{node3}) via MPI-based sampling. Each node independently computed $\sigma(n)$ for its assigned subrange and reported all fixpoints and any values outside the expected target set.

\textbf{Evidence Summary.} The combined node results identified exactly four digit-factorial fixpoints, namely $n \in \{1, 2, 145, 40585\}$, matching the target set precisely. No unexpected fixpoints were found in any subrange, yielding a total record count of $4$ and a verdict of \emph{sufficient}.
```